"""
Routes package.

Holds Flask Blueprints for versioned HTTP endpoints (e.g., /api/v1/*).
Each module here should define a Blueprint and only perform request parsing/
response formatting, delegating business logic to controllers/services.
"""
